{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run / Debug (CMake target)",
      "type": "cppdbg",
      "request": "launch",
      "program": "${command:cmake.launchTargetPath}",
      "cwd": "${workspaceFolder}",
      "MIMode": "lldb",
      "windows": { "type": "cppvsdbg" }
    }
  ]
}
